
public enum GameState {
    MainMenu = 0,
    Running = 1,
    Paused = 2,
    Finished = 3,
    GameOver = 4
}

public enum ScarabState {
    Patrolling = 0,
    Chasing = 1,
    Attacking = 2
}

public enum WurmState {
    Idle = 0,
    Moving = 1,
    Attacking = 2
}